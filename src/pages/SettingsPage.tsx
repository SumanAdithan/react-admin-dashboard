import { Notification, Profile } from '@components';

const SettingsPage = () => {
    return (
        <>
            <Profile />
            <Notification />
        </>
    );
};

export default SettingsPage;
