import { Notification, Profile, Security } from '@components';

const SettingsPage = () => {
    return (
        <>
            <Profile />
            <Notification />
            <Security />
        </>
    );
};

export default SettingsPage;
